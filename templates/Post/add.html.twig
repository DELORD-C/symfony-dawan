{% extends 'base.html.twig' %}

{% block content %}
    <h1>Ajouter un Post</h1>
    {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_row(form.content) }}
        {{ form_row(form.author) }}
        {{ form_row(form.save, { 'label': 'Créer' }) }}
    {{ form_end(form) }}
    <a href="{{ path('app_post_list') }}">Retour</a>
{% endblock %}